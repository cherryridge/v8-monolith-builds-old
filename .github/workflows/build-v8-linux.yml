name: Build on Linux

on:
    workflow_call:
        inputs:
            commit:
                type: string
                required: true
    workflow_dispatch:
        inputs:
            commit:
                type: string
                required: true

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
          - run: echo 1